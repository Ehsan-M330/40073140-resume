# رزومه آنلاین تعاملی

این پروژه شامل یک رزومه آنلاین با قابلیت دریافت درخواست پروژه و مدیریت آن‌ها از طریق پنل ادمین است.

## ویژگی‌ها

- 📄 صفحه رزومه با HTML و CSS
- 📝 فرم درخواست پروژه
- 👥 نمایش تعداد کاربران آنلاین به صورت زنده (WebSocket)
- 🔐 پنل مدیریت با احراز هویت JWT
- 📊 مدیریت درخواست‌ها (مشاهده، حذف، تغییر وضعیت)

## ساختار پروژه

```
.
├── index.html          # صفحه رزومه اصلی
├── styles.css          # استایل‌های صفحه رزومه
├── backend/
│   ├── main.py         # سرور FastAPI
│   └── admin.html      # صفحه مدیریت ادمین
├── requirements.txt    # وابستگی‌های Python
└── README.md          # این فایل
```

## نصب و راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 2. راه‌اندازی سرور بک‌اند

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

سرور در آدرس `http://localhost:8000` اجرا می‌شود.

### 3. مشاهده مستندات API

پس از راه‌اندازی سرور، می‌توانید مستندات API را در آدرس‌های زیر مشاهده کنید:

- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

### 4. مشاهده صفحه رزومه

دو صفحه رزومه در دسترس است:

1. **صفحه انگلیسی** (`index.html`): برای استفاده عمومی
2. **صفحه فارسی** (`resume.html`): نسخه فارسی با فرم درخواست پروژه

فایل `index.html` یا `resume.html` را در مرورگر باز کنید. برای استفاده از فرم درخواست و نمایش کاربران آنلاین، سرور باید در حال اجرا باشد.

### 5. دسترسی به پنل ادمین

پس از راه‌اندازی سرور، به آدرس زیر بروید:

```
http://localhost:8000/admin
```

**اطلاعات ورود پیش‌فرض:**
- نام کاربری: `admin`
- رمز عبور: `admin123`



## استفاده از API

### ارسال درخواست پروژه

```bash
curl -X POST "http://localhost:8000/api/project-request" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "John Doe",
    "email": "john@example.com",
    "phone": "+1234567890",
    "project_description": "I need a web application",
    "project_type": "web-development",
    "budget_range": "1000-5000",
    "timeline": "2-3-months"
  }'
```

### ورود به سیستم (برای ادمین)

```bash
curl -X POST "http://localhost:8000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

### دریافت لیست درخواست‌ها

```bash
curl -X GET "http://localhost:8000/api/project-requests" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## متغیرهای محیطی

می‌توانید متغیرهای زیر را تنظیم کنید:

```bash
export SECRET_KEY="your-secret-key-here"
export ADMIN_USERNAME="your-admin-username"
export ADMIN_PASSWORD="your-admin-password"
```

## WebSocket

برای اتصال به WebSocket و دریافت تعداد کاربران آنلاین:

```javascript
const ws = new WebSocket('ws://localhost:8000/ws/online-users');

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('Online users:', data.count);
};
```